#ifndef Debug_hpp
#define Debug_hpp

#include <cstdint>
#include <string>

std::string ShowHex(uint8_t);
std::string ShowHex(uint16_t);
std::string ShowBin(uint8_t);
std::string ShowBin(uint16_t);

#endif
